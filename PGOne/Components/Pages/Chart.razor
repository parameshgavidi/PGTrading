@page "/chart"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Live Market chart is on Dashboard — keep /chart as a redirect for old links.
        Navigation.NavigateTo("/", replace: true);
    }
}
